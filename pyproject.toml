[project]
name = "recipe-site"
version = "0.2.0"
description = "A private recipe sharing site for friends and family."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bleach>=6.3.0",
    "django>=5.0,<6.0",
    "gunicorn>=22.0",
    "markdown>=3.10.2",
    "pillow>=10.0",
    "pyyaml>=6.0.3",
    "whitenoise>=6.6",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-django>=4.8",
    "ruff>=0.6",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
pythonpath = ["src"]
testpaths = ["src"]
python_files = ["tests.py", "test_*.py", "*_tests.py"]
# -ra: short summary of skips/xfail/errors/failures (also used by scripts/ci-test.sh)
addopts = ["-ra"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src"]
extend-exclude = ["**/migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B904"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
