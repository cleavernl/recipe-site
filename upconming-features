- Import recipe from URL
- Tag recipes and filter by tags
- Add a "make it" mode where you can check things off
    - We can store a log of who made recipes when
    - We can order the default sorting of the home page with the recipes made most recently