services:
  web:
    build:
      context: .
      dockerfile: Containerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - recipe_data:/app/data
      - recipe_media:/app/media

volumes:
  recipe_data:
  recipe_media:
